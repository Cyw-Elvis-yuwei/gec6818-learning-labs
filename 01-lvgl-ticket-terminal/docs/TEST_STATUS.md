# 验证状态

更新时间：2026-07-23 10:20

## 当前摘要

- 对应代码阶段或 Git 锚点：当前 workspace 的文档初始化阶段；无有效 Git commit 锚点。
- 最近一次有效验证：needs-info；当前记录没有可按 V 格式复核的用户原始构建/运行/真机结果。
- 验证总错误标志：1
- 当前结论：未执行当前版本验证。已有 `demo` 文件、发布包和历史说明只能证明产物/文档存在，不能证明当前源码功能正确。

## 待验证项

| 编号 | 环境 | 工作目录 | 命令或操作 | 预期结果 | 状态 |
| --- | --- | --- | --- | --- | --- |
| V-01 | Ubuntu/交叉编译环境 | 用户回传与当前源码对应的实际项目根目录 | 逐条执行 `make clean`、`make -j$(nproc)`、`file demo`、`ls -lh demo`，每条命令立即保存退出码 | 构建命令退出码为 0；`demo` 存在并由 `file` 识别为目标 ARM 可执行文件 | 未执行 |
| V-02 | 目标 Linux 开发板 | 用户回传实际部署目录，发布说明为 `/root/ticket_terminal` | 执行发布安装/启动路径，观察 800×480 登录页、字体、显示和触摸输入 | 程序启动，登录页可操作，未出现启动错误；用户回传原始结果 | 未执行 |
| V-03 | 目标 Linux 开发板 | `/root/ticket_terminal` 或用户确认的运行目录 | 管理员登录后新增 `cyw / 88 / 10 / img_product_01`，查询并重启后复核 | 不出现 `buffer overflow`；新增商品可查询，重启后仍存在 | 未执行 |
| V-04 | 目标 Linux 开发板 | 用户回传实际运行目录 | 运行 `docs/TEST_FLOW.md` 中的登录、购买、付款、管理员 CRUD 和循环稳定性流程 | 用户观察到的页面、键盘、数据、重启和稳定性均符合流程；每项保留摘要 | 未执行 |
| V-05 | 目标 Linux 开发板 | 用户回传实际运行目录 | 注册弹窗确认/取消、软键盘收起和重复打开操作 | 不发生崩溃或段错误，注册/取消状态正确 | 未执行 |

## 已发生的验证

当前没有可以完整记录执行者、环境、工作目录、实际命令、原始退出码和产物证据的有效验证记录。`.scratch/keyboard-register-dialog-fix.md` 与 `.scratch/verify-after-board/verify-file-goods-buffer-overflow.md` 作为历史线索保留，但不移入已发生验证。

## 最近一次全量回归

- 状态：未执行 / needs-info。
- 对应代码阶段：未在当前记录中保留。
- 验证后源码是否再次变化：未在当前记录中保留。
- 能够证明：无。
- 不能证明：当前源码的编译、目标板启动、完整功能、数据持久化或循环稳定性。

## 统一回传格式

用户回传时先给摘要，再给必要原始输出：

```text
验证总错误标志：<0 或 1>
```

| 编号 | 环境 | 原始退出码 | 错误标志 | 文件或产物证据 | 结论 |
| --- | --- | ---: | ---: | --- | --- |
| V-01 | <环境> | <立即保存的退出码> | <0 或 1> | <存在性、最新修改时间、大小、类型或哈希> | <通过/失败/未执行> |

PowerShell 使用 `$exitCode = $LASTEXITCODE`，Linux/WSL 使用 `exit_code=$?`；任何报错、超时、证据缺失或无法确认都使错误标志为 1。文件时间、大小或类型只能证明产物存在，不能单独证明功能通过。
